{{#partial "content"}}
    <p>Content:</p>
    <p>{{ post.content }}</p>
    <h5>Created At:</h5>
    <p> {{ post.createdAt }}</p>
    <h5>Post status:</h5>
    <p>
        {{#if post.published }}
            <span class="glyphicon glyphicon-star" aria-hidden="true"></span>  Published
        {{else}}
            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Draft
        {{/if}}

        <a href="/posts/{{post.id}}/update"><button class="btn btn-primary">Edit post</button></a>
        <a href="/posts/{{post.id}}/delete"><button class="btn btn-danger">Delete post</button></a>
    </p>
{{/partial}}

{{> layout.hbs}}